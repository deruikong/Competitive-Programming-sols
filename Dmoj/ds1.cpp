#include "bits/stdc++.h"
#define scan(x) do{while((x=getchar())<'0'); for(x-='0'; '0'<=(_=getchar()); x=(x<<3)+(x<<1)+_-'0');}while(0)
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;
char _;
// BIT REEEEEEE
int n,
	A[mn], S[mn]; // T is THE BIT
int main(){
	cin >> n;
	for(int i = 1; i <= n; i++) cin >> A[i];
	return 0;
}